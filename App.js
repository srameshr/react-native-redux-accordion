import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import Accordion from './src/App';

export default class App extends React.Component {
  render() {
    return (
        <Accordion />
    );
  }
}
